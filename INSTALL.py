"""
BITOS OS - УСТАНОВКА (ПРОСТО И ПОНЯТНО)
Пошаговая инструкция установки
"""

print("""
╔══════════════════════════════════════════════════════════════════╗
║                  УСТАНОВКА BITOS OS V2.0                         ║
║              Полная инструкция для чайников                      ║
╚══════════════════════════════════════════════════════════════════╝

ШАГ 1: УСТАНОВИТЬ PYTHON
═════════════════════════════════════════════════════════════════

Windows:
  1. Скачать Python с https://www.python.org/downloads/
  2. При установке ОБЯЗАТЕЛЬНО галочка "Add to PATH"
  3. Установить

Linux (Ubuntu/Debian):
  sudo apt update
  sudo apt install python3 python3-pip

Mac:
  brew install python3

Проверка (в терминал):
  python --version
  pip --version


ШАГ 2: УСТАНОВИТЬ УТИЛИТЫ ДЛЯ ПРОГРАММИРОВАНИЯ ESP32
═════════════════════════════════════════════════════════════════

Открыть терминал (командную строку) и выполнить:

  pip install esptool
  pip install adafruit-ampy

Проверка:
  esptool.py version
  ampy --version


ШАГ 3: ПОДКЛЮЧИТЬ ESP32 К КОМПЬЮТЕРУ
═════════════════════════════════════════════════════════════════

1. Подключить ESP32 через USB кабель
2. Компьютер должен распознать устройство
3. Дождать установки драйверов (Windows)

Найти порт в терминале:

Linux/Mac:
  ls /dev/ttyUSB*
  или
  ls /dev/ttyACM*
  
  Результат: /dev/ttyUSB0 (или /dev/ttyUSB1 и т.д.)

Windows:
  1. Правый клик "This PC" → Manage
  2. Device Manager → Ports (COM & LPT)
  3. Должен быть "USB-SERIAL CH340" или похоже
  4. Написано COM номер (например COM3)
  
  Если ничего не видно - установить драйвер:
  https://github.com/julimelson/CH340-Drivers


ШАГ 4: СТЕРЕТЬ ПАМЯТЬ ESP32
═════════════════════════════════════════════════════════════════

В терминал выполнить (меняете /dev/ttyUSB0 на ваш порт):

  esptool.py --chip esp32 --port /dev/ttyUSB0 erase_flash

Результат: зелёный прогресс-бар
Ждать ~30-60 секунд
Должно написать: "Hard resetting via RTS pin..."


ШАГ 5: УСТАНОВИТЬ MICROPYTHON
═════════════════════════════════════════════════════════════════

1. Скачать прошивку:
   https://micropython.org/download/esp32/
   
   Взять файл: esp32-20230426-v1.20.0.bin
   (или новее, последний)

2. Положить файл в папку где вы работаете

3. В терминал выполнить (меняете имя файла если нужно):

   esptool.py --chip esp32 --port /dev/ttyUSB0 write_flash -z 0x1000 esp32-20230426-v1.20.0.bin

Результат: зелёный прогресс-бар
Ждать ~1-2 минуты
Должно написать: "Leaving... Hard resetting via RTS pin..."


ШАГ 6: ЗАГРУЗИТЬ BITOS
═════════════════════════════════════════════════════════════════

1. Скачать код Bitos:
   git clone https://github.com/garipoter2099-source/Bitos.git
   
   Или скачать ZIP с GitHub и распаковать

2. Перейти в папку Bitos:
   cd Bitos

3. Загрузить основной файл (меняете порт на ваш):
   
   ampy --port /dev/ttyUSB0 put bitos_complete.py

Результат: должно загрузиться быстро (2-5 сек)


ШАГ 7: ЗАГРУЗИТЬ BOOT.PY (автозапуск)
═════════════════════════════════════════════════════════════════

В терминал:

  ampy --port /dev/ttyUSB0 put boot.py

Это позволит Bitos запускаться автоматически при включении ESP32


ШАГ 8: ПЕРВЫЙ ЗАПУСК
═════════════════════════════════════════════════════════════════

В терминал:

  ampy --port /dev/ttyUSB0 run bitos_complete.py

РЕЗУЛЬТАТ:
  - На дисплее должна появиться анимация (100 квадратиков)
  - Потом инициализация систем
  - Потом заставка BITOS v2.0
  - Потом главное меню

ЕСЛИ НИЧЕГО НЕ ВИДНО:
  1. Проверить подключение дисплея (GPIO21, GPIO22)
  2. Проверить адрес I2C (0x3c или 0x3d)
  3. Перезагрузить ESP32 (нажать кнопку RESET)


ШАГ 9: ПРОВЕРКА ОБОРУДОВАНИЯ (опционально)
═════════════════════════════════════════════════════════════════

Проверить дисплей:
  ampy --port /dev/ttyUSB0 run -c "
import machine
i2c = machine.I2C(scl=machine.Pin(22), sda=machine.Pin(21))
print('I2C devices:', [hex(x) for x in i2c.scan()])
"

Должно вывести: I2C devices: ['0x3c'] (или 0x3d)

Проверить кнопку UP (GPIO32):
  ampy --port /dev/ttyUSB0 run -c "
from machine import Pin
btn = Pin(32, Pin.IN, Pin.PULL_UP)
print('GPIO32 value:', btn.value())
"

Нажать кнопку - должно измениться значение (1→0)


ШАГ 10: ВСЁ ГОТОВО!
═════════════════════════════════════════════════════════════════

✓ ESP32 работает
✓ Дисплей работает
✓ Bitos OS установлена
✓ Всё подключается автоматически

Наслаждайтесь вашей ОС! 🚀


═══════════════════════════════════════════════════════════════════
ПРОБЛЕМЫ И РЕШЕНИЯ
═══════════════════════════════════════════════════════════════════

❌ ОШИБКА: "esptool.py: not found"
✓ РЕШЕНИЕ: pip install --upgrade esptool

❌ ОШИБКА: "Permission denied /dev/ttyUSB0"
✓ РЕШЕНИЕ (Linux): sudo usermod -a -G dialout $USER
  (Потом переподключиться)

❌ ОШИБКА: "Failed to connect to /dev/ttyUSB0"
✓ РЕШЕНИЕ:
  1. Проверить USB кабель
  2. Перезагрузить ESP32 (нажать RESET)
  3. Попробовать другой USB порт
  4. Проверить драйверы (особенно Windows)

❌ Дисплей чёрный
✓ РЕШЕНИЕ:
  1. Проверить питание 3.3V на дисплей
  2. Проверить SCL/SDA на GPIO21/GPIO22
  3. Попробовать другой адрес I2C (0x3d вместо 0x3c)

❌ Кнопки не работают
✓ РЕШЕНИЕ:
  1. Проверить резисторы 10kΩ на pull-up
  2. Проверить GPIO номера
  3. Убедиться что кнопка замыкается на GND

═══════════════════════════════════════════════════════════════════
ПОЛЕЗНЫЕ КОМАНДЫ ПОСЛЕ УСТАНОВКИ
═══════════════════════════════════════════════════════════════════

Посмотреть файлы на ESP32:
  ampy --port /dev/ttyUSB0 ls

Удалить файл:
  ampy --port /dev/ttyUSB0 rm bitos_complete.py

Перезагрузить ESP32:
  ampy --port /dev/ttyUSB0 run -c "import machine; machine.reset()"

Очистить память:
  ampy --port /dev/ttyUSB0 run -c "import gc; gc.collect(); print('OK')"

═══════════════════════════════════════════════════════════════════
КОНТАКТЫ И ПОМОЩЬ
═══════════════════════════════════════════════════════════════════

GitHub репозиторий:
https://github.com/garipoter2099-source/Bitos

Вопросы? Создать Issue на GitHub!

═══════════════════════════════════════════════════════════════════

ГОТОВО! 🎉 Ваша Bitos OS установлена и работает!
""")
